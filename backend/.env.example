# Copy to backend/.env and fill with your keys and connection details

# --- AI Providers ---
COHERE_API_KEY=
GEMINI_API_KEYS=
ANTHROPIC_API_KEY=
AI_PROVIDER_FAILURE_TIMEOUT=300

# --- External APIs ---
# YouTube is required for video search/play features
YOUTUBE_API_KEY=

# --- Pinecone ---
PINECONE_API_KEY=
PINECONE_ENVIRONMENT=us-east-1

# --- Databases ---
# If using local Mongo, prefer MONGO_URI; DATABASE_URL is a fallback
MONGO_URI=mongodb://localhost:27017
MONGO_DB=assistant_db
DATABASE_URL=
# Local dev defaults; if using docker-compose services, use bolt://neo4j:7687
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password123

# --- Redis ---
# Local dev (if running Redis locally). If using docker-compose, set host=redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# --- Auth ---
SECRET_KEY=
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

# --- Optional Feature Flags ---
AI_ENABLE_HEDGED=true
AI_HEDGE_DELAY_MS=90
AI_MAX_PARALLEL=2

# --- Backend base URL (for internal callbacks, optional) ---
# BACKEND_BASE_URL=http://localhost:8000

# --- CORS / Dev Diagnostics ---
# Enable automatic learning of http://localhost:* origins (development only)
CORS_DYNAMIC_LOCAL=1
# Emit per-request CORS logs
DEBUG_CORS=1
# Optionally specify explicit origins (comma separated)
# CORS_ORIGINS=http://localhost:3000,http://localhost:3001
# To allow ALL origins (dev only) uncomment below
# CORS_ALLOW_ALL=1
